<Page x:Class="Safety_Wheel.Pages.Teacher.TeacherMainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
      xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
      xmlns:viewModels="clr-namespace:Safety_Wheel.ViewModels" 
      xmlns:core="clr-namespace:Safety_Wheel.Converters"
      xmlns:views="clr-namespace:Safety_Wheel.Pages.Teacher"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      Title="HamburgerMenuPage"
      Loaded="Page_Loaded">
    <!--xmlns:localViews="clr-namespace:Safety_Wheel.Views" <Page.DataContext> <viewModels:MainViewModel /> </Page.DataContext>-->
    <Border Background="{StaticResource White}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.Resources>
                <core:SelectedItemToContentConverter x:Key="SelectedItemToContentConverter" />
                <!-- this is the template for the items (options too) -->
                <core:MenuTypeToSingleVisibilityConverter x:Key="MenuTypeToSingleVisibilityConverter" />
                <core:MenuTypeToMultiVisibilityConverter x:Key="MenuTypeToMultiVisibilityConverter" />
                <core:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
                <DataTemplate x:Key="MenuItemTemplate" DataType="{x:Type viewModels:MenuItemViewModel}">
                    <Grid x:Name="RootGrid" Height="90" Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type mah:HamburgerMenu}}, Path=CompactPaneLength}" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <ContentControl Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Content="{Binding Icon}" Focusable="False" />
                        <TextBlock Grid.Column="1" VerticalAlignment="Center" FontSize="16" Text="{Binding Label}" />
                    </Grid>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type mah:HamburgerMenu}}, Path=IsPaneOpen}" Value="False">
                            <Setter TargetName="RootGrid" Property="ToolTip" Value="{Binding ToolTip, Mode=OneWay}" />
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>

                <DataTemplate x:Key="MenuItemTemplateLittle" DataType="{x:Type viewModels:MenuItemViewModel}">
                    <Grid x:Name="RootGrid" Height="50" Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type mah:HamburgerMenu}}, Path=CompactPaneLength}" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <ContentControl Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Content="{Binding Icon}" Focusable="False" />
                        <TextBlock Grid.Column="1" VerticalAlignment="Center" FontSize="16" Text="{Binding Label}" />
                    </Grid>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type mah:HamburgerMenu}}, Path=IsPaneOpen}" Value="False">
                            <Setter TargetName="RootGrid" Property="ToolTip" Value="{Binding ToolTip, Mode=OneWay}" />
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
                
                <!-- these are the templates for the view models -->
                <DataTemplate DataType="{x:Type viewModels:TeacherSelectedStudViewModel}"/>
                <DataTemplate DataType="{x:Type viewModels:SelectedDateViewModel}"/>
                <DataTemplate DataType="{x:Type viewModels:CreateTestsViewModel}"/>
                <DataTemplate DataType="{x:Type viewModels:StatisticsViewModel}"/>
            </Grid.Resources>
            <mah:HamburgerMenu Grid.Column="0"
                               x:Name="MainMenuControl" 
                               DisplayMode="CompactInline" 
                               IsPaneOpen="False" 
                               
                               ItemsSource="{Binding MainMenuItems}"
                               ItemTemplate="{StaticResource MenuItemTemplate}"                                
                               OptionsItemsSource="{Binding MenuOptionItems}"
                               OptionsItemTemplate="{StaticResource MenuItemTemplate}"
                               
                               ItemInvoked="HamburgerMenu_ItemInvoked"
                               SelectedItem="{Binding SelectedMainMenuItem, Mode=TwoWay}" 
                               SelectedIndex="0"
                               HamburgerVisibility="Collapsed" 
                               Style="{StaticResource MainMenuStyle}" 
                               DockPanel.Dock="Left"/>

            <mah:HamburgerMenu Grid.Column="1"
                               x:Name="PeopleMenuControl"
                               DisplayMode="CompactInline" 
                               IsPaneOpen="False" 
                               
                               ItemsSource="{Binding MenuItems}"
                               ItemTemplate="{StaticResource MenuItemTemplateLittle}" 
                               
                               SelectedItem="{Binding SelectedStudent, Mode=TwoWay}"
                               SelectedIndex="0"
                               HamburgerVisibility="Collapsed" 
                               Style="{StaticResource PeoplesMenu}"
                               Visibility="{Binding SecondMenuVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>
<!--ItemInvoked="DatesMenuControl_ItemInvoked"-->
            <mah:HamburgerMenu x:Name="DatesMenuControl" Grid.Column="2"
                               
                               DisplayMode="CompactInline" 
                               IsPaneOpen="False" 
                               
                               ItemTemplate="{StaticResource MenuItemTemplateLittle}" 
                               ItemsSource="{Binding MenuDatesItems}"                                
                               OptionsItemsSource="{Binding MenuOptionDateItems}"
                               OptionsItemTemplate="{StaticResource MenuItemTemplateLittle}"
                               ItemInvoked="HamburgerMenu_ItemInvoked"
                               
                               SelectedItem="{Binding SelectedDate, Mode=TwoWay}" SelectedIndex="0" 
                               HamburgerVisibility="Collapsed" 
                               Style="{StaticResource DatesMenu}" DockPanel.Dock="Left">
                <mah:HamburgerMenu.Visibility>
                    <MultiBinding Converter="{StaticResource MenuTypeToMultiVisibilityConverter}" ConverterParameter="Dates">
                        <Binding Path="SelectedMainMenuItem.Tag"/>
                        <Binding Path="MenuDatesItems.Count"/>
                    </MultiBinding>
                </mah:HamburgerMenu.Visibility>
            </mah:HamburgerMenu>
            <Grid Grid.Column="3">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Frame x:Name="FrameTeacher"
                           Grid.Column="1" 
                           Grid.Row="0"
                           NavigationUIVisibility="Hidden" 
                           HorizontalAlignment="Center" 
                           VerticalAlignment="Top"
                           Source="TeacherWelcomePage.xaml"/>

                    <!--Попытки для прохождений-->
                    <DataGrid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3"
                              Height="220"
                              ItemsSource="{Binding MenuAttemptsItems}"
                              SelectedItem="{Binding SelectedAttempt}"
                              SelectionUnit="FullRow"
                              SelectionMode="Single"
                              AutoGenerateColumns="False"
                              HeadersVisibility="Column"
                              Visibility="{Binding AttemptsTableVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <EventSetter Event="PreviewMouseLeftButtonDown"
                                             Handler="DataGridRow_PreviewMouseLeftButtonDown"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#334A90E2"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>


                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Время"
                                         Binding="{Binding Tag.StartedAt, StringFormat=HH:mm}" />

                            <DataGridTextColumn Header="Результат"
                                            Binding="{Binding Tag.Score}" />

                            <DataGridTextColumn Header="Тест"
                                            Binding="{Binding Tag.Test.Name}" />

                            <DataGridTextColumn Header="Предмет"
                                            Binding="{Binding Tag.Subject.Name}" />
                        </DataGrid.Columns>
                    </DataGrid>

                    <!--Тесты для статистики-->
                    <DataGrid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3"
                              Height="220"
                              IsReadOnly="True"
                              ItemsSource="{Binding MenuTestsItems}"
                              SelectedItem="{Binding SelectedTest}"
                              SelectionUnit="FullRow"
                              SelectionMode="Single"
                              AutoGenerateColumns="False"
                              HeadersVisibility="Column"
                              Visibility="{Binding StatisticTableVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <EventSetter Event="PreviewMouseLeftButtonDown"
                         Handler="DataGridRow_PreviewMouseLeftButtonDown"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#334A90E2"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>


                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Название теста"
                                                Binding="{Binding Tag.Name, StringFormat=HH:mm}" />
                            
                            <DataGridTextColumn Header="Предмет"
                                                Binding="{Binding Tag.Subject.Name}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Page>